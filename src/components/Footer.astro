---
---

<!-- TODO: confirm repo name once the GitHub remote exists -->
<footer class="footer">
	<p>Designed and <a href="https://github.com/latechlogan/portfolio">built by hand</a> · 2026</p>
</footer>

<style>
	.footer {
		border-top: 1px solid var(--border);
		padding-top: var(--space-16);
		padding-bottom: var(--space-8);
		text-align: center;
	}

	.footer p {
		max-width: var(--measure);
		margin-inline: auto;
		font-size: var(--text-sm);
		color: var(--text-muted);
	}

	.footer a {
		color: inherit;
		text-decoration: underline;
		text-underline-offset: 0.15em;
	}

	.footer a:hover {
		color: var(--green-ink);
	}
</style>
